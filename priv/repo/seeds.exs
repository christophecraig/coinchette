# Script for populating the database. You can run it as:
#
#     mix run priv/repo/seeds.exs
#
# Inside the script, you can read and write to any of your
# repositories directly:
#
#     Coinchette.Repo.insert!(%Coinchette.SomeSchema{})
#
# We recommend using the bang functions (`insert!`, `update!`
# and so on) as they will fail if something goes wrong.

# Seeds seront ajoutés une fois les schémas Ecto créés (T2.x)
# Pour l'instant, les tables sont créées et prêtes à être utilisées

IO.puts("✅ Database schema ready!")
IO.puts("📝 Seeds will be added in T2.x when Ecto schemas are implemented")
